services:
  jokerseed:
    build: .
    image: jokerseed:latest
    container_name: jokerseed
    restart: unless-stopped
    network_mode: host
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
